sudo: required
services:
  - docker

jobs:
    include:
        - stage: "Build"
          script: "docker build --tag dislib ."
        - stage: "Test"
          script: "docker run dislib bash run_tests.sh"

after_script:
  - docker images
