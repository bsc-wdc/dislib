sudo: required
language: python
python: 
  - "3.6" 
services:
  - docker

script:
  - docker build --tag dislib .
  - docker run dislib bash /home/dislib/run_tests.sh

after_script:
  - docker images
